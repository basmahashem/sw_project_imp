
import java.util.Scanner;
import javax.swing.JFrame;

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author Nour
 */
/*public class Main{
   
    public static void main(String[] args){
        // TODO code application logic here
        //ShelveFunctions SF = new ShelveFunctions();
        //SF.setVisible(true);
        //BookFunctions BF = new BookFunctions();
        //BF.setVisible(true);
        /*ReviewFunctions rf=new ReviewFunctions();
        //Database db2=new Database();
        int choice;
        int repeat=1;
        Scanner in=new Scanner(System.in);
        //db.getFirstName();
        System.out.println("to add a review,comment on a review,like a review (press 1)");
        choice=in.nextInt();
        if(choice==1)
        {
            new ReviewFunctions().setVisible(true);
        }
        //System.out.println("need to do any other proces: 1-yes , 2-no ");
        //repeat=in.nextInt();

        BookController BC = new BookController();
        BC.View();

    }
}
*/


import com.mysql.jdbc.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.Scanner;

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

/**
 *
 * @author Nour
 */
public class Main {
    public static void main(String[] args) {
    // TODO code application logic here
        //new HomePage().setVisible(true);
        //String FName , LName , Pass;
        String Name , Type , State ;
        //Database DB = new Database();
        /*DB.getFirstName();
        DB.getLastName();
        DB.getPassword();
        Scanner Scan = new Scanner(System.in);
        System.out.println("enter values:");
        FName = Scan.nextLine();
        LName = Scan.nextLine();
        Pass =  Scan.nextLine();*/
       // DB.InsertInfo(FName , LName , Pass);
        //Database DB = new Database();
        //DB.UpdateInfo(FName , null , Pass);
        //DB.DeleteInfo();
       // Scanner Scan = new Scanner(System.in);
        //DB.InsertShelve(Name ,Type , State);
        ShelveController SC = new ShelveController();
        //Scanner Scan = new Scanner(System.in);
        //Name = Scan.nextLine();
        //Type   = Scan.nextLine();
        //State = Scan.nextLine();
       // SC.Create(Name, Type, State);
        //SC.Edit(Name, Type, State);
   //     SC.Delete();
          /**
     * @param args the command line arguments
     */
    
        // TODO code application logic here
       // new HomePage().setVisible(true);
       
        

   

    /**
     * @param args the command line arguments
     */
    /**
     * @param args the command line arguments
     */
   
        // TODO code application logic here
       // new HomePage().setVisible(true);
       
        

   

    /**
     * @param args the command line arguments
     */
   
   new HomePage().setVisible(true);
  reviewfunctions rf=new reviewfunctions();
  //Database db2=new Database();
  int choice;
  int repeat=1;
  Scanner in=new Scanner(System.in);
//  db.getFirstName();
 
     // System.out.println("to add a review,comment on a review,like a review,display review (press 1)");
      //System.out.println("to add book,or delete book (press 2)"); 
      //choice=in.nextInt();
    
 //if(choice==1){new reviewfunctions().setVisible(true);}
 //if(choice==2){new BookFunctions().setVisible(true);}
//if(choice==3){new ShelveFunctions().setVisible(true);}
//if(choice==4){new GroupFunctions().setVisible(true);}
//if(choice==5){new Info().setVisible(true);}
//  System.out.println("need to do any other proces: 1-yes , 2-no ");
  // repeat=in.nextInt();
  }
    
  
    }
